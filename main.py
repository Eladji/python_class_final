# Description: This is the main file that runs the program. It creates an instance of the GUI class and runs the program.
from classes.GUI import GUI

    
if __name__ == "__main__": 
    GUI = GUI()
    GUI.run()   
